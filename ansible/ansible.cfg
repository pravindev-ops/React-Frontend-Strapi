[defaults]
inventory = inventory.ini
host_key_checking = False
pipelining = True
gather_facts = False

[ssh_connection]
# This allows Ansible to pass the custom SSH config from the environment
ssh_args = -C -o ControlMaster=auto -o ControlPersist=600s