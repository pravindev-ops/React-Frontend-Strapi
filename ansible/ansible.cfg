[defaults]
inventory = inventory.ini
host_key_checking = False

[ssh_connection]
# This is the magic line that enables IAP tunneling
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ProxyCommand="gcloud compute ssh --zone={{ zone }} {{ inventory_hostname }} --tunnel-through-iap --dry-run"