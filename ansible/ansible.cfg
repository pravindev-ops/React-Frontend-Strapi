[defaults]
inventory = inventory.ini
host_key_checking = False
pipelining = True
gather_facts = False

[ssh_connection]
# config-ssh creates the ProxyCommand, we just add the multiplexing
ssh_args = -o ControlMaster=auto -o ControlPersist=600s