version: '3.8'
services:
  strapi:
    image: ${STRAPI_IMAGE}
    restart: always
    env_file: .env
    ports:
      - "1337:1337"
    volumes:
      - strapi_uploads:/opt/app/public/uploads

  # frontend:
  #   image: ${FRONTEND_IMAGE}
  #   restart: always
  #   ports:
  #     - "80:3000"
  #   environment:
  #     - BACKEND_URL=http://${VM_IP}:1337

volumes:
  strapi_uploads: